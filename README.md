used the csv file "all_data", transformed it by changing some types and creating columns from columns and loading it into snowflake.
for the analyses i queries the data from snowflake to be used in python
